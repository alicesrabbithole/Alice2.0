# Byte-compiled / optimized / C extensions
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
.venv/
venv/
ENV/

# IDE and editor directories
.vscode/
.idea/

# Dotenv environment variables file
.env

# --- Alice Bot Specific ---

# Ignore everything under data by default
data/*

# Keep some important data files tracked
!data/.gitkeep
!data/stickers.json
!data/buildables.json
!data/rumble_listener_config.json

# Stocking assets:
# - ignore generated per-user composites
data/stocking_assets/*_user_*.png
# - ignore everything under stocking_assets, but explicitly allow the buildables subtree
data/stocking_assets/**
!data/stocking_assets/buildables/
!data/stocking_assets/buildables/**

# Keep runtime / ephemeral / backup files ignored
data/collected_pieces.json
data/*.bak
puzzles/*/meta.json.bak
utilities.json

# Web UI (if you want to ignore it for now)
web/*

# Backups and backup files
backups/
*.bck

# Puzzle pieces (binary assets) - adjust as needed
/puzzles/*/pieces/

# Logs / dbs / caches
*.log
*.sqlite3
*.db